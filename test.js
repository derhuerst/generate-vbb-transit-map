'use strict'

const test = require('tape')
const fs = require('fs')
const path = require('path')

const svg = fs.readFileSync(path.join(__dirname, 'map.svg'), {encoding: 'utf8'})

test('todo', (t) => {
})
